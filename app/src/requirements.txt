# Wind Turbine Digital Twin pipeline - Python dependencies
# Install with: pip install -r requirements.txt

confluent-kafka==2.4.0

# NOTE: the PyPI package for the Apache IoTDB Python native client is
# published as "apache-iotdb" (imported in code as `from iotdb...`),
# not "iotdb" -- "iotdb==1.3.3" does not exist on PyPI and pip install
# would fail. Pinned to the client version matching the apache/iotdb:1.3.3
# server image used in docker-compose.yml.
apache-iotdb==1.3.3

# .env file loading for scripts run outside Docker
python-dotenv==1.0.1
